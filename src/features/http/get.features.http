### Find all feature articles with pagination queries
# GET /features
# Sends an array of features.
GET {{localBaseUrl}}/features
?limit=10
&page=1
&orderBy=nameAsc

### Find a single film
# GET /films/:slug
# Sends a single film with Article, Person, Studio, Tag relations
GET {{localBaseUrl}}/films/the-americano-1916