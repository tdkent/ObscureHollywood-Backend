### Find all persons with pagination queries
# GET /persons
# Sends an array of persons.
GET {{localBaseUrl}}/persons
?limit=10
&page=1
&orderBy=lastNameAsc

### Find a single person
# GET /persons/:slug
# Sends a single film with relations
GET {{localBaseUrl}}/persons/ray-crash-corrigan